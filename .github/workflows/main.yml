name: Smart home

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get repository code
        uses: actions/checkout@v3
      - name: install dependencies
        run: npm ci
      - name: Run Tests
        run: npm run test
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Login in Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build docker
        run: docker build . --platform linux/arm64 -t vladimirovsk/smarthome
      - name: Publish to Docker
        run: |
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker push vladimirovsk/smarthome
################################################################
#I commented out this step because a real deploy to the server is not needed at this stage          
#  deploy:
#    needs: [build, test]
#    runs-on: ubuntu-latest
#    steps:
#      - name: Deploy to server
#        uses: appleboy/ssh-action@v0.1.4
#        with:
#          host: test-host
#          username: svladimirov
#          password: ${{ secrets.SSH_PASSWORD }}
##          key: ${{ secrets.SERVER_SECRET }}
#          port: 33100
#          script: |
#            cd project/smarthome
#            git checkout dev
#            git pull
#            docker-compose up -d --build
              
              
